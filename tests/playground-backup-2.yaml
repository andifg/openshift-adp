apiVersion: velero.io/v1
kind: Backup
metadata:
  name: playground-backup-2
  namespace: openshift-adp
spec:
  includedNamespaces:
    - playground
  includedResources:
    - pv
    - pvc
    - pod
  storageLocation: 'velero-1'